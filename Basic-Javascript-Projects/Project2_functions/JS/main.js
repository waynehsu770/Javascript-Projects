function First() {
    var str = 'This is the button text!';
    str += ' This is the end!'; // This concatenates the 2 strings thru the += operator
    document.getElementById('Button_Text').innerHTML = str; 
}